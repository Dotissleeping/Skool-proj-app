# assets/images

Place the real Skool logo here as:

    skool-logo.png

Referenced from:
- app.json (icon, splash, android adaptiveIcon)
- Settings > About screen (once built in Phase 11)

Until it's provided, the app uses a simple text-based placeholder in the UI —
no logo image is required for the app to build and run.